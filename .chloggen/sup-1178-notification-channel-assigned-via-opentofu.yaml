# Use this changelog template to create an entry for release notes.

# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, or a single word describing the area of concern (e.g. dashboards, check_rules, views)
component: notification_channels

# A brief description of the change. Surround your text with quotes ("") if it needs to start with a backtick (`).
note: Add a computed `id` attribute to `dash0_notification_channel` exposing the server-assigned channel id.

# Mandatory: One or more tracking issues related to the change. You can use the PR number here if no issue exists.
issues: [119]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use pipe (|) for multiline entries.
subtext: |
  Synthetic checks and check rules reference a notification channel by its server-assigned id, not by its `origin`.
  Use `dash0_notification_channel.<name>.id` to wire a channel into a synthetic check's `spec.notifications.channels`
  or a check rule's `dash0.com/notification-channel-ids` annotation. Previously there was no supported way to obtain
  the channel id, and stripping the `tf_` prefix off `origin` produced a different UUID that the backend silently
  failed to resolve.

# If your change doesn't affect end users or the exported elements of any package,
# you should instead start your pull request title with "chore" or use the "Skip Changelog" label.
# Optional: The change log or logs in which this entry should be included.
# e.g. '[user]' or '[user, api]'
# Include 'user' if the change is relevant to end users.
# Default: '[user]'
change_logs: []
